"use client";
import React, { ReactNode } from "react";

function PerfectScrollbarContainer({ children }: { children: ReactNode }) {
  return <div>{children}</div>;
}

export default PerfectScrollbarContainer;
